<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "Http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="account.dao.AccountDAO">
	<insert id="insert" parameterType="accountVO">
      	insert into b_account(ID, BANK_NAME, ACCOUNT_NUM, BALANCE,NICK_NAME)
      	values(#{id}, #{bank_name}, #{account_num}, #{balance},#{nick_name})
    </insert> 
    <select id="selectAll" resultType="accountVO" parameterType="string" >
    	select * from b_account where id = #{id}
    </select>
</mapper>